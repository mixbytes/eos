language: cpp
compiler:
- clang
- gcc
services:
  - docker
before_install:
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin
script:
  - docker build -t mixbytes/haya:latest-builder Docker/builder
  - docker push -t mixbytes/haya:latest-builder
